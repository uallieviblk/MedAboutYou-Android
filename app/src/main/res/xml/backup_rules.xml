<?xml version="1.0" encoding="utf-8"?>
<!-- Pre-API-31 Auto Backup rules. The Room database (schedules, dose log,
     stock) and DataStore prefs (user name, caregiver phone numbers) are health
     data: the app's privacy invariant is that they never leave the device, so
     they are excluded from Google's cloud backup. Use the in-app encrypted
     Backup & restore (Settings) to migrate devices. -->
<full-backup-content>
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="datastore/" />
</full-backup-content>
